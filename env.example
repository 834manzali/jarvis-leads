# FRONTEND-ONLY PROJECT - NO BACKEND VARIABLES NEEDED
# Jarvis Leads works entirely on the frontend with Telegram API

# No Supabase keys required
# No API keys required  
# No backend environment variables needed

# The application works out of the box without any configuration